# 🧠 AI SYSTEM INSTRUCTIONS FOR TELEPHONY-ACTION-SERVICE
1. **SDK Kullanımı:** Kendi içinde STT/TTS döngüsü barındırmaz. `sentiric-ai-pipeline-sdk` kullanmak ZORUNDADIR.
2. **Hata Yönetimi:** gRPC bağlantılarında (Media/Gateway) asla `unwrap()` kullanma. Hataları unwrap edip RMQ üzerinden `call.terminate.request` fırlat.
3. **mTLS:** Güvensiz (Insecure) gRPC bağlantıları anayasal ihlaldir.
